# Optional: frontend origin used by OAuth popup postMessage
POSTPANDA_FRONTEND_ORIGIN=http://127.0.0.1:5173

# OAuth callback base (must match redirect URIs in provider apps)
POSTPANDA_OAUTH_CALLBACK_BASE=http://127.0.0.1:8000

# Microsoft OAuth app
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_TENANT_ID=common

# Google OAuth app
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
